using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResourceManager : MonoBehaviour
{
    public long timeResourceAmount = 500;

    public long spaceResourceAmount = 500;

    public long substanceResourceAmount = 500;

    public long energyResourceAmount = 500;

    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
    
    
}